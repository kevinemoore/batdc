
<table class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <th>Contact</th>
    <th>Sponsor School</th>
    <th>Paid</th>
    <% @event.attendees.each do |a| %>
        <tr>
            <td><%= a.contact ? a.contact.full_name : 'Contact Not Found' %></td>
            <td><%= a.sponsor_school ? a.sponsor_school.name : 'None Specified' %></td>
            <td><%= a.paid %></td>
            <% btn_html = "btn btn-default" %>
            <td> <%= button_to 'Delete', a, method: :delete, data: {confirm: "Confirm?"}, class: btn_html %></td>
        </tr>
    <% end %>
</table>



